import { Skeleton } from '@nextui-org/react';
import styled from 'styled-components';

export const NameWrapperStyled = styled(Skeleton)`
  min-width: 66px;
  min-height: 16px;
  margin-bottom: 2px;
  border-radius: 10px;
`;

export const DescriptionWrapperStyled = styled(Skeleton)`
  min-width: 45px;
  min-height: 16px;
  border-radius: 10px;
`;
